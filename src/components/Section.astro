---
interface Props {
    title?: string;
}

const { title } = Astro.props;
---

<section class="xl:pt-4 xl:pb-4">
    {
        title && (
            <h2 class="text-center xl:text-3xl text-2xl xl:pb-32">{title}</h2>
        )
    }
    <slot />
</section>
