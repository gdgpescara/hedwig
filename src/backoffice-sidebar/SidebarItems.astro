---

/**
 * Sidebar list items
 * @prop content: elements that can be represented by a list 
 * @returns 
 */
interface Props {
  content: {
    item?: string;
    url?: string;
    icon?:string;
  }[]
}
const {content} = Astro.props;  

---

<ul class="menu h-full bg-base-200 text-base-content content-stretch justify-evenly">
    {content.map((item=><li key={item.item || item.icon}><div class="flex flex-row gap-5">
        <p>{item.icon}</p>
        <a href={item.url}>{item.item}</a>
        </div></li>))}
</ul>